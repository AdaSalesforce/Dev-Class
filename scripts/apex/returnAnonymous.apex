ReturnArthimaticalcalculation calc = new ReturnArthimaticalcalculation();
Integer x1 = 1;
Integer y1 = 2;
Integer t1 = 3;
Integer v1 = 4;

integer z1 = calc.addition(x1, y1);
system.debug('Sum of the two integer we assigned is ' + z1);
integer k1 = calc.addition(x1, y1, t1, v1);
system.debug('Sum of the two integer we assigned is ' + k1);

